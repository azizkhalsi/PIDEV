<form method="post" action="{{ path('app_type_cons_delete', {'id': type_con.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_con.id) }}">
    <button class="btn">Delete</button>
</form>
