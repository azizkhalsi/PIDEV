{% extends 'base.html.twig' %}

{% block title %}Conseil{% endblock %}

{% block body %}
    <h1>Conseil</h1>

    <table class="table">
        <tbody>
         
            <tr>
                <th>Description</th>
                <td>{{ conseil.description }}</td>
            </tr>
            <tr>
                <th>Typecons</th>
                <td>{{ conseil.typecons }}</td>
            </tr>
        </tbody>
    </table>

    

    

    {{ include('conseil/_delete_form.html.twig') }}
<a class="btn btn-lg btn-primary" href="{{ path('app_conseil_edit', {'id': conseil.id}) }}">Modifier</a>
     <a  class="btn btn-lg btn-primary" href="{{ path('app_reponse_index1') }}">Reponse</a>
     <a  class="btn btn-lg btn-primary" href="{{ path('app_conseil_index') }}">back to list</a>
{% endblock %}
